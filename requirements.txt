# ComfyUI Updated Pack - Requirements
# Compatible with transformers 4.x and 5.0.0+

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy<2
Pillow>=9.4.0

# Transformers (compatible with both older and newer versions)
transformers

# For SAM2 and GroundingDINO
timm>=0.4.12
addict
yapf
pyyaml
tqdm>=4.66.1
hydra-core>=1.3.2
omegaconf
iopath>=0.1.10
matplotlib>=3.9.1
opencv-python>=4.7.0
supervision

# For WAS Node Suite
cmake
fairscale>=0.4.4
gitpython
imageio
joblib
numba
opencv-python-headless
pilgram
rembg
scikit-image>=0.20.0
scikit-learn
scipy
requests

# Optional but recommended packages
# image-reward  # Uncomment for ImageReward support (will use CLIP as fallback if not installed)

# WAS Suite optional dependencies (install from source if needed)
# git+https://github.com/WASasquatch/img2texture.git
# git+https://github.com/WASasquatch/cstr
# git+https://github.com/WASasquatch/ffmpy.git
